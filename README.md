# Final Bounce — site statique (GitHub Pages)

## Déploiement rapide
- Mets **tout le contenu de ce dossier à la racine** du dépôt GitHub Pages.
- Active GitHub Pages sur la branche (souvent `main`) et le dossier `/ (root)`.

## Ce qui a été corrigé
- Les pages HTML sont à la racine (GitHub Pages veut `index.html` au root).
- Les liens d’assets ont été rendus relatifs (plus de `../`).
- Les attributs Webflow qui déclenchent l’ajout automatique du badge/logo (`data-wf-domain`, `data-wf-status`) ont été retirés.

## Notes
- Si tu veux un domaine custom: configure-le dans GitHub Pages et ajoute un fichier `CNAME` au root.
